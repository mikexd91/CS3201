1 - uses-procedure
procedure p;
Select p such that Uses("firstProc", "a")
firstProc, pokemon, ABC
5000
2 - uses-procedure
procedure p;
Select p such that Uses(p, "c")
firstProc, ABC
5000
2 - uses-boolean

Select BOOLEAN such that Uses("firstProc", "a")
true
5000
4 - uses-stmt
stmt s;
Select s such that Uses(s, "c")
7, 9, 11, 12, 14, 16, 43, 44, 48, 50, 55, 56
5000
5 - uses-procedure-pattern: procedure synonym, variable fixed
procedure p;
Select p such that Uses(p, "a") pattern a("c", _)
firstProc, ABC
5000
6 - uses-variable: procedure fixed, variable fixed
variable v;
Select v such that Uses("firstProc", v) and Uses("ABC", v)
a, c
5000
